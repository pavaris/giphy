import React from 'react';

const Favorites = (props) => {
  return(
    <div id="favorites">
      <div className="content-margins">list of favorite gifs</div>
    </div>
  )
}

export default Favorites;
